name: cordon
version: 0.5.1
license: MPL2
description: |
  A Crystal shard for running shell commands within a
  platform-native "cordon", with a configurable access policy

crystal: ">= 1.0.0"

authors:
  - nogginly <nogginly@icloud.com>

development_dependencies:
  ameba:
    github: crystal-ameba/ameba

targets:
  cordon:
    main: src/cordon_cli.cr
